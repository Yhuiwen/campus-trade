# 手工测试清单

状态说明：未实际执行的用例统一标为“待验证”，执行通过后再改为“通过”。

| 模块 | 测试场景 | 操作步骤 | 预期结果 | 状态 |
| --- | --- | --- | --- | --- |
| 用户模块 | 注册 | 进入注册页，填写唯一用户名、昵称和密码后提交 | 注册成功，默认角色为 `USER` | 待验证 |
| 用户模块 | 登录 | 使用初始化账号或新账号登录 | 返回 token，前端进入首页并显示用户信息 | 待验证 |
| 用户模块 | JWT 失效后访问受保护接口 | 清理或篡改 localStorage 中的 token 后访问 `/api/auth/me` | 后端返回 401，前端跳转登录 | 待验证 |
| 用户模块 | 管理员访问后台 | 使用 `admin` 登录后访问 `/admin` | 可以进入后台页面 | 待验证 |
| 用户模块 | 普通用户访问后台 | 使用普通用户访问 `/admin` 或 `/api/admin/users` | 前端重定向或后端返回 403 | 待验证 |
| 商品模块 | 发布商品 | 登录后进入 `/publish`，填写分类、标题、描述、价格和图片 | 商品创建成功，状态为 `PENDING` | 待验证 |
| 商品模块 | 上传图片 | 在发布或编辑商品时选择 jpg/jpeg/png 图片上传 | 返回图片 URL，页面可预览图片 | 待验证 |
| 商品模块 | 商品列表搜索 | 在首页输入关键词或选择分类 | 只展示符合条件且状态为 `ON_SALE` 的商品 | 待验证 |
| 商品模块 | 商品详情 | 打开 `/goods/{id}` | 展示商品、卖家、分类、价格、浏览量和收藏状态 | 待验证 |
| 商品模块 | 编辑商品 | 发布者进入我的商品并编辑未售出商品 | 商品信息更新，状态重新变为 `PENDING` | 待验证 |
| 商品模块 | 下架商品 | 发布者下架自己的未售出商品 | 商品状态变为 `OFF_SHELF` | 待验证 |
| 商品模块 | 管理员审核商品 | 管理员在后台通过待审核商品 | 商品状态变为 `ON_SALE` | 待验证 |
| 商品模块 | 管理员驳回商品 | 管理员在后台驳回待审核商品 | 商品状态变为 `REJECTED` | 待验证 |
| 订单模块 | 买家下单 | 买家打开非本人 `ON_SALE` 商品详情并创建订单 | 订单状态为 `CREATED`，商品状态变为 `LOCKED` | 待验证 |
| 订单模块 | 不能购买自己的商品 | 发布者尝试购买自己的商品 | 后端拒绝并提示不能购买自己的商品 | 待验证 |
| 订单模块 | 不能重复购买锁定或已售商品 | 对 `LOCKED` 或 `SOLD` 商品再次下单 | 后端拒绝并提示商品当前不可购买 | 待验证 |
| 订单模块 | 买家取消订单 | 买家取消 `CREATED` 状态订单 | 订单变为 `CANCELLED`，商品恢复 `ON_SALE` | 待验证 |
| 订单模块 | 卖家确认完成 | 卖家确认 `CREATED` 状态订单完成 | 订单变为 `COMPLETED`，商品变为 `SOLD` | 待验证 |
| 评价与信用 | 完成订单后评价 | 买家对 `COMPLETED` 订单提交 1-5 星评价 | 评价创建成功，卖家信用分按规则变化 | 待验证 |
| 评价与信用 | 重复评价限制 | 对同一订单再次提交评价 | 后端拒绝，提示订单已评价 | 待验证 |
| 评价与信用 | 未完成订单评价 | 对 `CREATED` 或 `CANCELLED` 订单提交评价 | 后端拒绝，提示订单完成后才能评价 | 待验证 |
| 评价与信用 | 信用分边界 | 多次提交低分或高分评价 | 信用分不会低于 0，也不会高于 150 | 待验证 |
| 收藏与浏览历史 | 收藏商品 | 登录后在商品详情页点击收藏 | 收藏成功，按钮状态变为已收藏 | 待验证 |
| 收藏与浏览历史 | 取消收藏 | 对已收藏商品点击取消收藏 | 收藏记录删除，按钮状态同步变化 | 待验证 |
| 收藏与浏览历史 | 查看我的收藏 | 打开 `/my/favorites` | 展示当前用户收藏的商品 | 待验证 |
| 收藏与浏览历史 | 浏览详情产生历史 | 登录后访问商品详情页 | `/my/history` 中出现该商品 | 待验证 |
| 收藏与浏览历史 | 重复浏览更新时间 | 多次访问同一商品详情 | 浏览历史不重复新增，只更新时间并影响排序 | 待验证 |
| 数据看板 | 统计用户数 | 管理员访问后台统计概览 | 用户总数展示正确 | 待验证 |
| 数据看板 | 统计商品数 | 管理员查看商品统计 | 商品总数和状态分布展示正确 | 待验证 |
| 数据看板 | 统计订单趋势 | 管理员查看最近 7 日订单趋势 | 折线图展示日期和订单数 | 待验证 |
| 数据看板 | 统计成交金额 | 完成订单后查看概览 | 成交金额按 `COMPLETED` 订单累计 | 待验证 |
| 智能交易助手 | 价格评估 | 调用 `/api/agent/price-advice` 或在页面填写估价信息 | 返回建议价格区间和理由 | 待验证 |
| 智能交易助手 | 风险分析 | 登录后查看商品风险分析 | 返回风险等级、分数、原因和建议 | 待验证 |
| 智能交易助手 | 个性化推荐 | 登录后产生收藏或浏览行为，再访问推荐 | 推荐优先匹配用户偏好分类，并排除本人商品 | 待验证 |
| 智能交易助手 | 发布建议 | 在发布页填写商品信息并生成建议 | 返回优化标题、描述、建议价格和完整度建议 | 待验证 |
| 智能交易助手 | 规则问答 | 进入 `/agent` 提交购买、价格或信用分相关问题 | 返回规则版回答，不修改业务数据 | 待验证 |
